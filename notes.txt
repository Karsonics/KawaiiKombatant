╔══════════════════════════════════════════════════════════╗
║              KAWAIKOMBATANT MASTER TODO LIST              ║
╚══════════════════════════════════════════════════════════╝

────────────────────────────────────────────────────────────
  IMMEDIATE (no GPU / network needed)
────────────────────────────────────────────────────────────

[ ] Wire up Faster-Whisper or openai-whisper for ASR
    - server/process/asr_func/asr_push_to_talk.py is still empty
    - Use openai-whisper via PyTorch ROCm (Faster-Whisper is CUDA-only)
    - whisper.cpp + Vulkan is another option on AMD
    - Add push-to-talk keybinding (spacebar to record)

[ ] Replace regex entity extraction with LLM-based extraction
    - bot_main.py extract_user_info() is fragile regex
    - Send a small prompt to Ollama to extract structured data
    - More robust, catches natural language variations
    - Could batch extractions instead of per-message

[ ] Batch database writes
    - Currently saves memory to MySQL on EVERY extraction
    - Accumulate changes, flush every N seconds or M changes
    - Reduces DB load significantly

[ ] Conversation summarization
    - When context exceeds N messages, summarize old ones
    - Inject summary as a system message instead of raw history
    - Lets you keep far more context without overflowing the window

[ ] Add multi-user support
    - user_id is hardcoded to "default_user"
    - Add --user flag to bot_main.py
    - Let session manager operate per-user
    - Add user switching mid-session

[ ] Add a --dry-run flag to bot_main.py
    - Runs without Ollama/DB, tests config loading and regex extraction
    - Useful for quick iteration on regex patterns

[ ] Add a --voice flag for selecting TTS voice at startup
    - Currently reads from sovits_config.yaml only

────────────────────────────────────────────────────────────
  MEDIUM TERM (needs testing environment)
────────────────────────────────────────────────────────────

[ ] Web UI
    - Gradio or FastAPI + HTMX frontend
    - Real-time streaming of LLM responses
    - Voice recording via browser mic
    - Session browser + memory viewer
    - Character personality switcher from the UI

[ ] Docker Compose setup
    - ollama service (with ROCm device passthrough)
    - gptsovits service (GPT-SoVITS-AMD fork)
    - mysql service
    - bot service
    - One docker-compose up to start everything

[ ] Pluggable TTS backends
    - Abstract TTS behind an interface
    - Current: GPT-SoVITS
    - Want: Fish Speech, Piper, Coqui TTS, Edge TTS
    - Config-driven backend selection

[ ] Pluggable LLM backends
    - Abstract behind an interface
    - Current: OpenAI-compatible (Ollama)
    - Want: direct llama.cpp binding, vLLM, Anthropic, OpenAI

[ ] semantic memory / RAG pipeline
    - Vector store (ChromaDB, FAISS) for semantic recall
    - Instead of loading N raw messages, query relevant memories
    - Much more scalable for long-running conversations

[ ] End-to-end benchmark automation
    - tests/test_full_pipeline.py has stubs only
    - Wire up: ASR -> LLM -> TTS timing
    - Latency breakdown per component
    - Export to CSV/JSON like the TTS benchmarks

[ ] Character mood persistence
    - Currently the dynamic mood resets to neutral on new session
    - Store mood across sessions in user_preferences
    - Character "remembers" how the last conversation ended

────────────────────────────────────────────────────────────
  ARCH / AMD GPU SPECIFIC
────────────────────────────────────────────────────────────

[ ] Verify ROCm is actually being used
    - Add `rocm-smi` check at startup
    - Print GPU model, VRAM, ROCm version
    - Compare inference speed with/without GPU

[ ] GPU test improvements (tests/test_gpu.py)
    - Add `torch.version.hip` check
    - Add ROCm memory info
    - Test actual GPU inference, not just availability

[ ] Systemd user services
    - ollama.service (already exists with pacman)
    - mysql.service (already exists)
    - gptsovits.service (needs custom .service file)
    - Start all three with one command

[ ] Ollama performance tuning
    - Set OLLAMA_NUM_GPU=999 in systemd override
    - Set HSA_OVERRIDE_GFX_VERSION for your GPU
    - Verify offloading with `ollama ps`
    - Compare Q4_K_M vs Q8_0 vs FP16 for your VRAM

[ ] GPT-SoVITS server as a systemd service
    - Write /etc/systemd/system/gptsovits.service
    - Include ROCm env vars
    - Auto-restart on crash

[ ] Audio output device selection
    - sounddevice uses default device (might be HDMI audio)
    - Add device selection to sovits_config.yaml
    - List available devices with python -m sounddevice

────────────────────────────────────────────────────────────
  CODE QUALITY & INFRASTRUCTURE
────────────────────────────────────────────────────────────

[ ] pre-commit hooks
    - Black formatter
    - Ruff linter
    - mypy type checker
    - pytest for tests

[ ] GitHub CI
    - .github/workflows/ci.yml
    - Run lint + typecheck + tests on push
    - Test on multiple Python versions (3.10, 3.11, 3.12)

[ ] Add Makefile
    - make run (starts the bot)
    - make test (runs all tests)
    - make lint (ruff check)
    - make typecheck (mypy)
    - make clean (removes __pycache__, logs, etc.)

[ ] Pydantic config models
    - Replace raw yaml.safe_load() with Pydantic models
    - Automatic validation + IDE autocomplete
    - One model per config file

[ ] structured logging improvements
    - Add log rotation (currently grows unbounded)
    - Add JSON log format option (for log aggregation)
    - Add request IDs per user message for tracing

────────────────────────────────────────────────────────────
  NICE TO HAVE
────────────────────────────────────────────────────────────

[ ] Emotional TTS
    - Pass mood from compute_character_state to TTS
    - Some TTS backends support emotion tags
    - Kuro sounds sad when mood is "annoyed"

[ ] Custom wake word
    - Porcupine or openWakeWord
    - Bot listens for "Hey Kuro" to start recording
    - Full hands-free mode

[ ] Telegram / Discord bot
    - Wrap the chat loop in a bot framework
    - Same Kuro character, but accessible from phone
    - Persistent per-user sessions

[ ] Voice activity detection (VAD)
    - Silero VAD or WebRTC VAD
    - Auto-detect when user stops speaking
    - No push-to-talk needed

[ ] Export conversation as PDF / HTML
    - Pretty formatted chat log
    - Timestamps, character name, avatar
    - Good for sharing conversations

────────────────────────────────────────────────────────────
  COMPLETED
────────────────────────────────────────────────────────────

[x] Delete old_files/ directory
[x] Fix hardcoded audio path in sovits_config.yaml
[x] Replace print() with logging everywhere
[x] Add retry decorator to API calls
[x] Type hints across all modules
[x] Config validation at startup
[x] Unit tests for extract_user_info() regex
[x] Dynamic character state based on sentiment
[x] Session import functionality
[x] README overhaul with quickstart + troubleshooting
[x] notes.txt converted to proper todo list
[x] Phase 0: Server architecture refactor
    [x] KuroEngine class (server/kuro_engine.py)
    [x] FastAPI + WebSocket server (server/kuro_api.py)
    [x] bot_main.py → thin WS client with --direct fallback
    [x] Updated requirements.txt (fastapi, uvicorn, websockets)
    [x] Updated README.md with server docs + WS protocol
[x] Phase 1: Voice pipeline
    [x] voice/mic.py — MicRecorder (sounddevice-based audio capture)
    [x] voice/vad.py — VoiceActivityDetector (Silero VAD with energy fallback)
    [x] voice/asr.py — WhisperTranscriber (openai-whisper, lazy-loaded)
    [x] voice/voice_client.py — Push-to-talk orchestrator + WS integration
    [x] bot_main.py — --voice flag added
    [x] Updated requirements.txt (whisper, pynput, silero-vad)
    [x] Updated README.md with voice docs
[x] SECURITY: GitHub secret leak remediation
    [x] Scrubbed code.py and token from all git history (git-filter-repo)
    [x] Also scrubbed configs/database_config.yaml (MySQL creds)
    [x] Pre-commit hook installed (.git/hooks/pre-commit — blocks tokens/secrets)
    [x] .gitignore updated (added token, code.py, *discord*, *bot_token*)
[ ] PENDING: Revoke Discord token in Discord Developer Portal
[ ] PENDING: gh auth login + git push origin --force --all
[x] Phase 2: VTube Studio avatar integration
    [x] configs/vtube_config.yaml — VTS host/port, expression maps, idle anims
    [x] avatar/emotion_map.py — config loader with env var overrides
    [x] avatar/vtube_controller.py — VTS WebSocket handler (auth, hotkeys, models)
    [x] avatar/avatar_client.py — KuroAPI WS listener + VTS expression sync
    [x] server/kuro_api.py — mood broadcast to subscribed clients
    [x] bot_main.py — --avatar flag added
     [x] Updated README with avatar setup + model sources

────────────────────────────────────────────────────────────
  TTS BACKEND ROADMAP
────────────────────────────────────────────────────────────

[x] GPT-SoVITS v1 (gsv-v2final) — primary backend
    [x] Mood-based reference audio selection (per-mood clips)
    [x] Speed/temperature per-mood tuning (happy=faster, annoyed=slower)
    [x] Config-driven mood mapping in sovits_config.yaml
    [x] Per-mood reference clips directory: assets/voices/moods/

[x] GPT-SoVITS v2ProPlus — installed but API-mode overhead negates speed gains
    [x] Updated to latest main (April 2026) — CUDA graph support
    [x] v2ProPlus weights activated (s2Gv2ProPlus.pth + s1v3.ckpt)
    [ ] Test with persistent session (WebUI) where CUDA graph persists
    [ ] Strip API overhead — direct Python library call instead of HTTP

[x] F5-TTS (optimized) — secondary backend for emotional variety
    [x] Custom FastAPI server on port 5050 (/mnt/storage/f5_tts/)
    [x] torch.compile enabled (reduce-overhead mode)
    [x] AOTriton SDPA enabled (TORCH_ROCM_AOTRITON_ENABLE_EXPERIMENTAL=1)
    [x] nfe_steps default 16 (halved from 32, 2x faster)
    [x] cfg_strength 1.0 (was 2.0)
    [x] AMD patches applied (soundfile instead of torchcodec, DiT case fix)
    [x] Speed: 0.21x RTF after optimizations (5x real-time)
    [ ] FP16 conversion blocked — mixed-precision incompatibility (fix later)
    [ ] Add per-mood reference audio config to f5_tts.py
    [ ] F5-TTS systemd service for auto-start

[x] Qwen3-TTS — tertiary backend (port 8880, Docker)
    [x] MIOpen fix attempted (cudnn.enabled=False) — no improvement
    [x] Flash-attention CK build blocked — needs GPU during Docker build
    [x] SDPA with AOTriton confirmed as fastest on RDNA4
    [ ] Upgrade nuraci fork to latest (fix compile_talker API mismatch)
    [ ] Test torch.compile with fixed API version

[x] Fish Speech S2-Pro — model downloaded, Docker built, OOM on 16GB
    [x] Imagilux AMD ROCm fork cloned at /mnt/storage/fish_speach_amd_new_test/
    [x] miopen-conv-fix active (88 conv layers patched)
    [x] Model: fishaudio/s2-pro (11GB) downloaded
    [ ] Needs 24GB+ GPU (4B model + KV cache + decoder = ~13GB minimum)
    [ ] Re-enable when GPU upgraded: docker compose -f compose.yml -f compose.rocm.yml up

[x] Pluggable TTS backend abstraction
    [x] TTSBackend abstract class (server/process/tts_func/base.py)
    [x] GPTSovitsBackend (server/process/tts_func/gpt_sovits.py)
    [x] F5TTSBackend (server/process/tts_func/f5_tts.py)
    [x] TTS manager with routing (server/process/tts_func/__init__.py)
    [x] Per-mood routing: neutral/curious→GPT-SoVITS, happy/annoyed/excited→F5-TTS
    [x] Smart fallback: F5-TTS down → GPT-SoVITS handles all moods
    [x] Backward-compat shim in sovits_amd.py
    [x] bot_config.yaml TTS config: mode, mood_routing, backends

[x] Comparative benchmark suite
    [x] bench_sovits_vs_qwen3.py (tests/) — 27 sentences × 8 categories
    [x] JSON/CSV/TXT export to benchmark_results/
    [x] Per-backend + merged comparison
    [x] Results: GPT-SoVITS v1 0.68x RTF (winner), v2 1.01x, Qwen3 1.67x

[ ] Future TTS models to test (ranked by priority)
    [ ] ZipVoice (k2-fsa, 123M, Apache 2.0)
        - Flow-matching, smallest model, Chinese+English
        - Should be fastest on AMD, <1GB VRAM
        - pip install zipvoice or clone from GitHub
    [ ] CosyVoice 3 (FunAudioLLM, 0.5B, Apache 2.0)
        - Built-in FastAPI server — instant integration
        - 9 languages + 18 Chinese dialects, streaming 150ms
        - AMD PR #1280 exists, needs onnxruntime workaround
        - Test: clone FunAudioLLM/CosyVoice-ttsfrd
    [ ] VoxCPM2 (openbmb, 2B, Apache 2.0)
        - Voice design FROM TEXT (no audio needed!) + voice cloning
        - 30 languages, 48kHz, commercial OK
        - ~8GB VRAM at bf16 — fits in 16GB
        - Tokenizer-free diffusion autoregressive
    [ ] OmniVoice (k2-fsa, ~1B, Apache 2.0)
        - 600+ languages, RTF 0.025, voice cloning + design
        - If it works on ROCm → game-changer
    [ ] GLM-TTS (zai-org, Apache 2.0)
        - Best WER on benchmarks (0.89), RL emotion control
        - Streaming, Chinese+English, phoneme control
    [ ] MOSS-TTS-Nano (OpenMOSS, 0.1B, Apache 2.0)
        - ONNX version runs on CPU, 48kHz stereo
        - No GPU needed — useful as fallback

[ ] Productionize TTS stack
    [ ] F5-TTS systemd service (port 5050, auto-restart)
    [ ] GPT-SoVITS systemd service (port 9880, auto-restart)
    [ ] Qwen3-TTS systemd service (Docker auto-restart existing)
    [ ] Docker Compose for all 3 TTS + Ollama + bot
    [ ] VRAM budget monitoring (ollama 6GB + GPT-SoVITS 4GB + F5-TTS 3GB = 13GB/16GB)
    [ ] Health check watchdog per backend
    [ ] Auto-fallback if any backend goes down

[ ] Emotion/accent reference audio collection
    [ ] Record/generate 5 Kuro reference clips:
        [ ] assets/voices/moods/happy.wav
        [ ] assets/voices/moods/annoyed.wav
        [ ] assets/voices/moods/excited.wav
        [ ] assets/voices/moods/curious.wav
        [ ] assets/voices/moods/neutral.wav
    [ ] Source options:
        - Record yourself doing Kuro impressions
        - Extract from anime VAs on sounds-resource.com
        - Generate with Fish Speech S2-Pro emotion tags [excited] etc.
        - Use Qwen3-TTS voice design to generate mood variants
    [ ] Update sovits_config.yaml mood refs when clips exist

[x] TTS COMPLETED
    [x] Sentence-streaming TTS (kuro_engine.py _stream_response)
    [x] Mood system analyzes AI response (not just user input)
    [x] Dynamic memory injection per-turn
    [x] Config tweaks: speed_factor 1.0, context=10, summarization=15
    [x] WS streaming per-sentence (kuro_api.py)
    [x] Per-mood reference audio selection (GPT-SoVITS)
    [x] F5-TTS installed, optimized, benchmarked
    [x] Dual-TTS backend with per-mood routing
    [x] Comparative benchmark suite
